import React from "react";
import propTypes from "prop-types";

export default function Logo({ children, ...props }) {
  return <img className="img-fluid  " src=" /logo.png" />;
}

Logo.propTypes = {};
